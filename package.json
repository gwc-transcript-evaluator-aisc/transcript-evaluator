{
  "name": "summer-camp",
  "version": "0.1.0",
  "private": true,
  "description": "Summer Camp - Course catalog and articulation platform",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present",
    "clean": "npm run clean --workspaces --if-present && rm -rf node_modules"
  },
  "engines": {
    "node": ">=22.0.0",
    "npm": ">=10.0.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/yourusername/summer-camp.git"
  },
  "keywords": [
    "course-catalog",
    "articulation",
    "education",
    "bedrock",
    "aws"
  ],
  "author": "",
  "license": "MIT"
}
