# Runtime configuration
TRANSCRIPT_API_BASE_URL=https://transcripts.example.edu
TRANSCRIPT_API_AUTH_TOKEN=
# ARN of the Secrets Manager secret holding the generated shared API key.
ORCHESTRATOR_API_KEY_SECRET_ARN=
CATALOG_TABLE_NAME=
RUNS_TABLE_NAME=
WORK_TABLE_NAME=
RESULTS_TABLE_NAME=
CATALOG_CACHE_TABLE_NAME=
STATE_MACHINE_ARN=
EVALUATOR_FUNCTION_ARN=
BEDROCK_MODEL_ID=

# Local stacks (CDK context local=true) explicitly bypass API-key verification.
# Non-local stacks create the secret and grant its read permission only to ApiHandler.
