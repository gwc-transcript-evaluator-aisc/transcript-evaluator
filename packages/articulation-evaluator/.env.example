# Populated by the CDK stack's Lambda environment at deploy time. Only needed for
# local invocation against deployed resources.
CATALOG_TABLE_NAME=
EVALUATIONS_TABLE_NAME=
BEDROCK_MODEL_ID=us.anthropic.claude-sonnet-5
